= render("form")

%fieldset
  %legend= t(".legend")
  %ol
    %li
      %span.label= t(".created_at")
      = l(@user.created_at)
    %li
      %span.label= t(".updated_at")
      = l(@user.updated_at)